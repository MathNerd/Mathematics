

    pvx = point_velocity(1);
    pvy = point_velocity(2);
    pvxs = sign(pvx);
    pvys = sign(pvy);
    pavx = abs(pvx);
    pavy = abs(pvy);
    
    rvx = rectangle_velocity(1);
    rvy = rectangle_velocity(2);
    rvxs = sign(rvx);
    rvys = sign(rvy);
    ravx = abs(rvx);
    ravy = abs(rvy);
    
% TO DO :::::::::::::
    
% contact_type = { LEFT_EDGE, TOP_EDGE, BOTTOM_LEFT_CORNER, ETC.... }
    
function resolve_no_contact()
    
endfunction

function resolve_contact()
    
endfunction

function resolve()

endfunction